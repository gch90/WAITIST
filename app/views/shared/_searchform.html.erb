<%= form_tag (current_user.nil? || current_user.user?) ? restaurants_path : waiters_path, method: :get do %>
  <%= text_field_tag :query,
    params[:query],
    class: "form-control",
    placeholder: "What are you looking for"
  %>
  <%= submit_tag "Search", class: "btn btn-primary" %>
<% end %>

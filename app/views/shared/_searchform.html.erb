<%= form_tag (current_user.nil? || current_user.waiter?) ? restaurants_path : user_path(tab: 3), class: "search", method: :get do %>
  <%= text_field_tag :query,
    params[:query],
    class: "input",
    placeholder: "Search..."
  %>
  <%= submit_tag "", class: "btn-search" %>
<% end %>

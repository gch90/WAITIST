<%= form_tag (current_user.nil? || current_user.waiter?) ? restaurants_path : waiters_path, class: "search", method: :get do%>
  <%= text_field_tag :query,
    params[:query],
    class: "input",
    placeholder: "What are you looking for"
  %>
  <%= submit_tag "", class: "btn-search" %>
<% end %>

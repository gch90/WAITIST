<%# REMINDER: CAN USE METHODS: %>
<%# created_applications, applications_including_restaurant, applications_including_waiter
waiter_applications_received, restaurant_applications_received, waiter?, restaurant? %>
<div class="d-flex" data-controller="pannel" data-pannel-chosen-value="<%= @tab %>">
  <%= render 'pannel' %>
  <div class="account" data-pannel-target="pages">
    <%# Order is very important! %>
    <%= render 'profile' %>
    <%= render 'application' %>
    <%= render 'chats', chatrooms: @chatrooms %>
    <%= render 'calendar' %>
    <%= render 'waiters' %>
  </div>

</div>

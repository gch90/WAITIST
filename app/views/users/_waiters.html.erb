<div class="d-none waiters"  >
  <h1><%= render "icons/waiters" %>Calendar</h1>
  <div class="sort">
    Sort-By
    <div class="btn btn-light">Rating</div>
  </div>
  <% @waiters.each do |waiter|%>
    <%= render "shared/waiterCard", wait: waiter %>
  <% end %>
</div>
